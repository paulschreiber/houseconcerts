<h1>Thanks for reserving a seat</h1>

<div class="pagetext">

<p><%= @rsvp.full_name %>, your <%= @rsvp.seats.humanize %><%= pluralize(@rsvp.seats, 'spot').gsub(@rsvp.seats.to_s, '') %> <%= @rsvp.seats > 1 ? 'have' :  'has' %> been reserved for the <strong><%= @rsvp.show.name %></strong> show on <strong><%= @rsvp.show.start.to_date.to_s(:long) %>.</strong></p>

<p>You’ll receive my address and directions via email in a day or so.</p>

</div>