# Settings.default_venue_group (config/settings.yml) is 2 - Person#ensure_venue_group
# looks it up by primary key, so a VenueGroup with id: 2 must always exist.
one:
  id: 1
  name: "Test Group One"

two:
  id: 2
  name: "Default Test Group"
